[Unit]
Description=Hadoop YARN NodeManager
After=network.target

[Service]
User={{ hadoop_user }}
Group={{ hadoop_group }}
Environment=HADOOP_CONF_DIR=/etc/hadoop
ExecStart=/bin/bash -lc '{{ hadoop_symlink }}/bin/yarn nodemanager'
Restart=on-failure

[Install]
WantedBy=multi-user.target
